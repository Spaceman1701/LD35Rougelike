using UnityEngine;
using System.Collections;

public class ShowTitle : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}


//Basically I'm going to make two text boxes with a background to the right of the hud.  When you mouse over one of the buttons, it will pass the ability name/description to the text box and change what is displayed.  When not moused over, I'll set its opacity to 0 so you won't see it.